# Development / test dependencies
-r requirements.txt

# Test tooling
pytest==8.3.4
# Pinned <0.28 because starlette's TestClient (bundled with fastapi 0.109) is
# incompatible with httpx 0.28+ ("Client.__init__() got an unexpected 'app'").
httpx==0.27.2